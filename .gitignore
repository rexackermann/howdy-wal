__pycache__/
# Ignore generated/active system files
/tmp/howdy_*
config.sh.local

# We want to keep the templates in the repo, but ignore their system-installed counterparts?
# Actually, the repo usually just has the templates.
# If they have .service extension, they will be blocked by *.service.
# Let's be explicit.
# Allow the templates in the root
!howdy-WAL.service
!00-howdy-WAL
!faceauth
